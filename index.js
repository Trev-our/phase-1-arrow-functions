const foo = function() {
    return 'bar';
  }
  const add = (a, b) => a + b;
  const twoAdder = x => x + 2;
  const nums = [1, 2, 3];
  const squares = nums.map(x => x ** 2);  // [1, 4, 9]
const divide = function() {
    return 2000 / 100;
}

const square = x => x * x;